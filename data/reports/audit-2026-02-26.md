# Portföy Tam Tarama Raporu — 26 Şubat 2026

## Özet
- GitHub’da görünen repo sayısı: 3 (`1ndirim`, `1ben`, `ops-admin`).
- GitHub’da görünen SSH key sayısı: 1 (`main birdir1`).
- GitHub’da görünen token sayısı: 3 (ops-admin, Cursor, 1ndirim-macbook).
- Firebase projeleri: `indirim-1`, `ben-2a979`, `birdir1-7049e`.
- `tar1f.*` domainleri DNS/Nginx/SSL’de yok (planlı ama kurulu değil).
- Sunucu kaynakları: RAM 5.8GiB, disk 148G (kullanım %16).

## Uygulamalar (net durum)

1. **1ndirim**
   - Repo: https://github.com/birdir1/1ndirim
   - Domain: `1ndirim.birdir1.com`, `api.1ndirim.birdir1.com`, `admin.1ndirim.birdir1.com`
   - Firebase: `indirim-1` (Android/iOS/Web)
   - Backend: PostgreSQL + Redis + Firebase Admin
   - Kullanıcı verisi: Firebase Auth + backend PostgreSQL (kampanya/yorum/puan) + Redis cache

2. **1ben**
   - Repo: https://github.com/birdir1/1ben
   - Domain: yok
   - Firebase: `ben-2a979` (Android/iOS hazır) → **kullanılacak**
   - Backend: yok (demo auth)
   - Kullanıcı verisi: cihazda `SharedPreferences` (lokal)
   - Karar: Firebase kullanılacak; auth provider’lar sonra açılacak.

3. **1look**
   - Repo: yok (local only)
   - Domain: yok
   - Firebase: yok
   - Kullanıcı verisi: kalıcı storage yok (şu an belirsiz)
   - Durum: uygulama var ama altyapı yok.

4. **1dovme**
   - Repo: yok (GitHub’da görünmüyor)
   - Domain: yok
   - Firebase: yok
   - Kullanıcı verisi: in-memory (MVP)
   - Durum: UI prototip, backend yok.

5. **tar1f**
   - Repo: yok (GitHub’da görünmüyor)
   - Domain: planlı (`tar1f.birdir1.com`, `api.tar1f.birdir1.com`, `admin.tar1f.birdir1.com`)
   - Firebase: `tar1f-prod` planlı (rezervasyon bekliyor)

## GitHub Erişim Analizi

- SSH key (GitHub):
  - `main birdir1` aktif (tek key görünüyor).
- SSH key (local):
  - `1ndirim_codex_key` aktif
  - `id_ed25519` belirsiz
  - `codex_1indirim_ed25519` legacy
- Tokenlar:
  - `ops-admin` (geniş scope, aktif) → kullanılmıyorsa daralt / yenile
  - `Cursor` (expired) → sil
  - `1ndirim-macbook` (unknown) → netleştir / sil

## Domain Durumu

- Aktif: `1ndirim.*`, `ops.birdir1.com`
- Yok: `1ben.*`, `1look.*`, `1dovme.*`, `tar1f.*`
- Not: DNS’te wildcard (`*`) var; ancak **Nginx ve SSL yoksa** domain çalışmaz.

## Firebase Durumu

- `indirim-1` → aktif, production
- `ben-2a979` → aktif, demo auth
- `birdir1-7049e` → kullanım belirsiz

## Sunucu Kapasite Notu

Mevcut kaynaklar (şu an):
- RAM: 5.8GiB (kullanılan 1.1GiB)
- Disk: 148G (kullanılan 22G)

Yük arttığında yapılacaklar (öncelik sırası):
1. İzleme: CPU/RAM/DB bağlantı sayısı, API latency
2. Cache oranını artır (Redis)
3. PostgreSQL index + read tuning
4. Uygulama servislerini ayrıştır (API + bot + ops)
5. Gerekirse daha büyük sunucuya geçiş

## birdir1 Klasör Tarama (boyut / durum)

- `1ndirim` — 1.7G — aktif (backend + admin + app)
- `1ben` — 586M — aktif
- `1dovme` — 1.2G — prototip
- `1look` — 458M — prototip
- `ops-admin` — 554M — aktif
- `ops` — 108K — legacy/unknown
- `backend__deprecated` — 24K — deprecated (arşivlenebilir)
- `tmp_campaigns.js` — 44K — geçici dosya (temizlenebilir)
- `.idea/.vscode/.cursor` — IDE ayarları (istersen repo dışı bırak)
